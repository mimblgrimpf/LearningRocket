import enum

from enum import Enum

class DIRECTION(Enum):
    CLOCKWISE = 1
    COUNTERCLOCKWISE = 2

class Propeller():
    maxRPM = 3000
    AirspeedRatio =
    TorqueRatio =
    Clockwise = False

